# syntax=docker/sandbox-kit:3
# OpenCode as a MIXIN: node plus the npm package in an overlay, with the
# same credentials, egress, and seeds as the opencode workload, minus what
# only the environment-owning kit carries (the entrypoint — run `opencode`
# in the base's shell — and the base's apt mirrors).
schemaVersion: "3"
displayName: OpenCode (mixin)
author: "Docker, Inc."
description: >-
  OpenCode as a mixin — node and the CLI in an overlay, with seven
  optional proxy-managed provider credentials. Layer it onto a shell base
  and run `opencode`.
sourceUrl: https://github.com/sst/opencode

kind: mixin

args:
  version:
    default: "0.2.28"
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    description: OpenCode release to install
    buildArg: OPENCODE_VERSION

provides: ["opencode@${{ kit.args.version }}"]

dockerfile: ./opencode-mixin.dockerfile

capabilities:
  # Every domain a credential injects into, the OpenAI OAuth endpoint and
  # its resource host, and the non-credential domains opencode needs (its
  # site/config schema, npm for MCP/plugin installs, GitHub release assets
  # for LSP auto-install — the githubusercontent zone via the documented
  # single-label wildcard, since assets 302 to its subdomains). The base's apt mirrors stay with
  # the base.
  - type: com.docker.sandbox/network-policy@1
    config:
      runtime:
        allow:
          - api.anthropic.com:443
          - claude.ai:443
          - console.anthropic.com:443
          - api.business.githubcopilot.com:443
          - api.enterprise.githubcopilot.com:443
          - api.github.com:443
          - api.githubcopilot.com:443
          - api.individual.githubcopilot.com:443
          - copilot.github.com:443
          - github.com:443
          - "*.githubusercontent.com:443"
          - raw.githubusercontent.com:443
          - codeload.github.com:443
          - aiplatform.googleapis.com:443
          - generativelanguage.googleapis.com:443
          - oauth2.googleapis.com:443
          - vertexai.googleapis.com:443
          - api.groq.com:443
          - api.openai.com:443
          - openai.com:443
          - auth.openai.com:443
          - chatgpt.com:443
          - openrouter.ai:443
          - api.x.ai:443
          - registry.npmjs.org:443
          - opencode.ai:443
          - "*.opencode.ai:443"
  # Multi-provider credentials, each optional and proxy-managed where a
  # well-known env var exists. The github credential mirrors the copilot
  # kit: one auth format per service, Bearer throughout.
  - type: com.docker.sandbox/credential@1
    optional: true
    description: Anthropic API access
    config:
      service: anthropic
      phase: runtime
      apiKey:
        name: ANTHROPIC_API_KEY
        proxyManaged: true
        inject:
          - {domain: api.anthropic.com, header: x-api-key, format: "%s"}
          - {domain: claude.ai, header: x-api-key, format: "%s"}
          - {domain: console.anthropic.com, header: x-api-key, format: "%s"}
  - type: com.docker.sandbox/credential@1
    optional: true
    description: GitHub and Copilot access
    config:
      service: github
      phase: runtime
      apiKey:
        # Named deliberately (v2 was inject-only): GITHUB_TOKEN is the
        # spelling gh and git tooling read natively, so the sentinel
        # doubles as ambient auth at the allowed hosts.
        name: GITHUB_TOKEN
        proxyManaged: true
        inject:
          - {domain: api.business.githubcopilot.com, header: Authorization, format: "Bearer %s"}
          - {domain: api.enterprise.githubcopilot.com, header: Authorization, format: "Bearer %s"}
          - {domain: api.github.com, header: Authorization, format: "Bearer %s"}
          - {domain: api.githubcopilot.com, header: Authorization, format: "Bearer %s"}
          - {domain: api.individual.githubcopilot.com, header: Authorization, format: "Bearer %s"}
          - {domain: copilot.github.com, header: Authorization, format: "Bearer %s"}
          - {domain: github.com, header: Authorization, format: "Bearer %s"}
          - {domain: raw.githubusercontent.com, header: Authorization, format: "Bearer %s"}
  - type: com.docker.sandbox/credential@1
    optional: true
    description: Google AI API access
    config:
      service: google
      phase: runtime
      apiKey:
        name: GOOGLE_GENERATIVE_AI_API_KEY
        proxyManaged: true
        inject:
          - {domain: aiplatform.googleapis.com, header: x-goog-api-key, format: "%s"}
          - {domain: generativelanguage.googleapis.com, header: x-goog-api-key, format: "%s"}
          - {domain: oauth2.googleapis.com, header: x-goog-api-key, format: "%s"}
          - {domain: vertexai.googleapis.com, header: x-goog-api-key, format: "%s"}
  - type: com.docker.sandbox/credential@1
    optional: true
    description: Groq API access
    config:
      service: groq
      phase: runtime
      apiKey:
        name: GROQ_API_KEY
        proxyManaged: true
        inject:
          - {domain: api.groq.com, header: Authorization, format: "Bearer %s"}
  # A ChatGPT-subscription token is only accepted at the Codex backend
  # host. OpenCode's codex auth plugin keys off an "openai" oauth entry in
  # auth.json, rewrites /v1/responses to chatgpt.com/backend-api/codex,
  # and sends the entry's access value as the bearer — the sentinel below
  # is what reaches the proxy for substitution.
  - type: com.docker.sandbox/credential@1
    optional: true
    description: OpenAI API access (API key or ChatGPT OAuth)
    config:
      service: openai
      phase: runtime
      apiKey:
        name: OPENAI_API_KEY
        proxyManaged: true
        inject:
          - {domain: api.openai.com, header: Authorization, format: "Bearer %s"}
          - {domain: openai.com, header: Authorization, format: "Bearer %s"}
      oauth:
        tokenEndpoint: {host: auth.openai.com, path: /oauth/token}
        resourceHosts:
          - chatgpt.com
        sentinels:
          accessToken: oai-oat01-proxy-managed
          refreshToken: oai-ort01-proxy-managed
        credentialFile:
          path: "~/.local/share/opencode/auth.json"
          structure:
            openai:
              type: oauth
              access: "{{.AccessToken}}"
              refresh: "{{.RefreshToken}}"
              expires: "{{.ExpiresAt}}"
  - type: com.docker.sandbox/credential@1
    optional: true
    description: OpenRouter API access
    config:
      service: openrouter
      phase: runtime
      apiKey:
        name: OPENROUTER_API_KEY
        proxyManaged: true
        inject:
          - {domain: openrouter.ai, header: Authorization, format: "Bearer %s"}
  - type: com.docker.sandbox/credential@1
    optional: true
    description: xAI API access
    config:
      service: xai
      phase: runtime
      apiKey:
        name: XAI_API_KEY
        proxyManaged: true
        inject:
          - {domain: api.x.ai, header: Authorization, format: "Bearer %s"}
  - type: com.docker.sandbox/lifecycle@1
    config:
      startup:
        # Gateway-first MCP registration, merged with jq rather than
        # overwritten: startup runs on every boot, and opencode.json is
        # the agent's main config, so truncating it would delete user
        # settings and other MCP servers. Deep merge updates only the
        # mcp.mcp-gateway entry; write-temp + rename keeps a crash from
        # leaving a half-written file. No-op when MCP isn't enabled.
        - command: |
            set -e
            [ -n "$MCP_GATEWAY_URL" ] || exit 0
            mkdir -p "$HOME/.config/opencode"
            cfg="$HOME/.config/opencode/opencode.json"
            frag=/tmp/sandbox-mcp-gateway.json
            jq -n --arg url "$MCP_GATEWAY_URL" --arg auth "Bearer $MCP_SENTINEL_TOKEN_NAME" \
              '{"$schema": "https://opencode.ai/config.json", mcp: {"mcp-gateway": {type: "remote", url: $url, enabled: true, headers: {Authorization: $auth}}}}' > "$frag"
            [ -f "$cfg" ] || echo '{}' > "$cfg"
            jq -s '.[0] * .[1]' "$cfg" "$frag" > "$cfg.tmp"
            mv "$cfg.tmp" "$cfg"
            rm -f "$frag"
          user: agent
          env: [MCP_GATEWAY_URL, MCP_SENTINEL_TOKEN_NAME]
          description: Register the sandbox MCP gateway by merging into opencode.json

  # Where opencode reads shared skills, so the host's store reaches it.
  - type: com.docker.sandbox/agent-skills@1
    optional: true
    config:
      path: /home/agent/.config/opencode/skills

  - type: com.docker.sandbox/agent-context@1
    config:
      contentFile: ./opencode-context.md
