# Gemini CLI

This sandbox runs Google's Gemini CLI. The API key is proxy-managed — the
value in the environment is a sentinel the egress proxy swaps at the
Google endpoints — and ~/.gemini/settings.json is seeded for sandbox use.
