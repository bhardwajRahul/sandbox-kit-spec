# Codex

This sandbox runs OpenAI's Codex CLI as its workload. Launch is `codex`
in dangerous-bypass mode; credentials arrive proxy-managed (API key or
ChatGPT OAuth), so tokens in local config files are sentinels, never
secrets.
