# Docker Agent

This sandbox runs Docker Agent, a multi-provider coding agent. Every
provider key is proxy-managed: the values in the environment are
sentinels the egress proxy swaps at each provider's endpoints.
