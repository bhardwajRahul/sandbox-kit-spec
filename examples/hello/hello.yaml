# syntax=docker/sandbox-kit:3
schemaVersion: "3"
displayName: Hello
author: "Christian Dupuis <cd@docker.com>"
description: Smallest possible sandbox kit, for smoke-testing the frontend

kind: workload
# Unversioned provides take their version from `version:` below — or from a
# version-shaped image tag at consumption, which always overrides it. A kit
# publishing an unversioned provide with neither fails the build.
provides: ["hello@1.0.0"]

args:
  greeting:
    default: world
    pattern: "^[a-z]+$"
    buildArg: GREETING

capabilities:
  - type: com.docker.sandbox/network-policy@2
    config:
      runtime:
        allow:
          # Everything httpbin answers except the destructive verbs, which
          # the smoke test has no use for and which make a refusal easy to
          # see: DELETE comes back 403 from the boundary, not 405 from the
          # origin.
          - hosts: [httpbin.org]
            methods: [GET, HEAD, OPTIONS, POST, PUT, PATCH]

  - type: com.docker.sandbox/agent-context@1
    config:
      filename: AGENTS.md
      contentFile: ./hello-context.md
