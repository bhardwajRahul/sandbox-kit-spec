{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "com.docker.sandbox/port@1",
  "description": "Config schema for one port need: an in-container port to publish. The host side of any binding is the host's answer, never part of the request. Instance-shaped, keyed by container port and transport. The @1 in the type names this schema; it moves when the schema does.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "container"
  ],
  "properties": {
    "name": {
      "type": "string"
    },
    "container": {
      "anyOf": [
        {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535
        },
        {
          "$ref": "../../definitions/kit-arg.schema.json#/definitions/whole"
        }
      ]
    },
    "transport": {
      "anyOf": [
        {
          "enum": [
            "tcp",
            "udp"
          ]
        },
        {
          "$ref": "../../definitions/kit-arg.schema.json#/definitions/bearing"
        }
      ]
    }
  }
}
