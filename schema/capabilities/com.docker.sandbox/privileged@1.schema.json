{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "com.docker.sandbox/privileged@1",
  "description": "The privileged-execution need takes no config: the request is present or absent, and the host may refuse it. This schema rejects every config value accordingly.",
  "not": {}
}