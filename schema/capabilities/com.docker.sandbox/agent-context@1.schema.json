{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "com.docker.sandbox/agent-context@1",
  "description": "Written instruction content the agent reads. filename is the profile a workload kit owns; mixins contribute contentFile or content alone (the two are mutually exclusive).",
  "type": "object",
  "additionalProperties": false,
  "minProperties": 1,
  "properties": {
    "filename": {
      "type": "string"
    },
    "contentFile": {
      "type": "string",
      "description": "Context-relative path in the authored descriptor; rewritten by the frontend to the staged in-image path in the published one."
    },
    "content": {
      "type": "string",
      "description": "Small instruction content inline, for content-free kits."
    }
  }
}