# syntax=docker/sandbox-kit:3
schemaVersion: "3"
kind: mixin
displayName: Required credential
description: Requires a credential outright, so refusing it when unclaimed is observable
capabilities:
  - type: com.docker.sandbox/credential@1
    config:
      service: kit-tck
      phase: runtime
      apiKey:
        name: KIT_TCK_TOKEN
