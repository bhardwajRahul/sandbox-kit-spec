# syntax=docker/sandbox-kit:3
schemaVersion: "3"
kind: mixin
displayName: Privilege
description: Requests elevation, so the grant is observable
capabilities:
  - type: com.docker.sandbox/privileged@1
