# syntax=docker/sandbox-kit:3
schemaVersion: "3"
kind: mixin
displayName: Required kit registry
description: Requires the runtime's kit registry outright, so refusing it when unclaimed is observable
capabilities:
  - type: com.docker.sandbox/kit-registry@1
