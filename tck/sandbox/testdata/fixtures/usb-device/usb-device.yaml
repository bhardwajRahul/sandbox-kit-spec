# syntax=docker/sandbox-kit:3
schemaVersion: "3"
kind: mixin
displayName: Required USB device
description: Requires a device passthrough outright, so refusing it when unclaimed is observable
capabilities:
  - type: com.docker.sandbox/usb-device@1
    config:
      vendorId: "0x1234"
      productId: "0x5678"
