# syntax=docker/sandbox-kit:3
schemaVersion: "3"
kind: mixin
displayName: Persistent volume
description: Declares a volume, so surviving a reboot is observable
capabilities:
  - type: com.docker.sandbox/volume@1
    config:
      path: /data
