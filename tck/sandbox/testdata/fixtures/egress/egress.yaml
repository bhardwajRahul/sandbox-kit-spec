# syntax=docker/sandbox-kit:3
schemaVersion: "3"
kind: mixin
displayName: Required egress
description: Requires network policy outright, so refusing it when unclaimed is observable
capabilities:
  - type: com.docker.sandbox/network-policy@1
    config:
      runtime:
        allow: [example.com]
