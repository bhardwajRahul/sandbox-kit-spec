# syntax=docker/sandbox-kit:3
schemaVersion: "3"
kind: mixin
displayName: Agent context
description: Stages a context body, so reaching it by its published path is observable
capabilities:
  - type: com.docker.sandbox/agent-context@1
    config:
      contentFile: ./context.md
