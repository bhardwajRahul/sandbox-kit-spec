# syntax=docker/sandbox-kit:3
schemaVersion: "3"
kind: mixin
displayName: Resource limits
description: Declares a memory limit, so what the sandbox enforces is observable
capabilities:
  - type: com.docker.sandbox/resources@1
    config:
      memory: 2g
